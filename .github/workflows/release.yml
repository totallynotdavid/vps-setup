name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: ./build
      - run: gh release create "$GITHUB_REF_NAME" dist/install.sh dist/install.sh.sha256 --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
